# Coding Guidelines

- Put library code in `src/` and tests in `spec/`.
- Use Crystal's formatter; do not hand-format around it.
- Add focused specs for public behavior and bug fixes.
- Avoid committing generated files or scratch output.
